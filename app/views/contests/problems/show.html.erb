<h1>Submit your output</h1>

<%= form_tag(action: 'submit') do %>
  <%= select_tag('input_type',
                 options_for_select({ 'small' => :small, 'large' => :large })) %>
  <%= text_area_tag('output') %>
  <%= submit_tag() %>
<% end %>
