<%= form_for(@image, :html => {multipart: true}) do |f| %>
  <%= f.error_messages %>

  <div class="field">
    <%= f.label :attachment %>
    <%= f.file_field :attachment %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
